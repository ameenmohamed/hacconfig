spring:
  application:
    name: springcloud
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ameenmohamed/hacconfig.git
security:
  user: 
   name: ameen
   password: fatima11 
      
management:
  context-path: /admin
  
logging:
  level:
    org.springframework.cloud: 'DEBUG'
